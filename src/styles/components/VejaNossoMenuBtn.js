import styled from "styled-components";
import Button from "../objects/Button";

const VejaNossoMenuBtn = styled(Button)`
  & {
    max-width: 300px;
    margin: var(--gap-md) auto 0 auto;
  }
`;

export default VejaNossoMenuBtn;
